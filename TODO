Function cloning

Help info in Loom controller

Loom controller client shouldn't exit after receiving the first message

Make DEBUG_APP_CONTROLLER configurable

Make limits configurable

mysql_execute_command uses a much larger stack frame when instrumented with loom

Perform unlock operations before lock operations

loom_view_proc.py is not able to list a Loom process whose name is longer than
15 characters.

conservatively mark blocking external call
